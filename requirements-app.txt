# AviShkar: API (api_server.py) + Gradio (app.py) + search.py + analyzer.py
# Use this file on Python 3.11–3.13 when the full requirements.txt fails (TensorFlow / old numpy pins).
# Install: pip install -U pip setuptools wheel
#          pip install -r requirements-app.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
requests>=2.31.0
google-generativeai>=0.8.0
gradio>=4.0.0
sentence-transformers>=3.0.0
